# 产品化改造任务清单

> 规则：每完成一项，就在这里勾掉一项。

## 第一阶段：本地启动体验产品化

- [x] 在项目中落地可持续维护的任务清单
- [x] 为终端用户提供正式本地启动入口，避免直接使用 `pnpm dev`
- [x] 为终端用户提供正式本地停止入口
- [x] 启动时自动检查环境、数据库与可用端口
- [x] 启动成功后自动打开浏览器
- [x] 启动失败时输出用户可理解的错误提示
- [x] 提供 Windows 免黑窗口启动入口

## 第二阶段：路径与运行时整理

- [x] 收敛运行时路径配置，支持显式指定数据与缓存目录
- [x] 为本地运行与 Docker 运行提供清晰的默认路径策略
- [x] 统一 Git 可执行文件探测与报错

## 第三阶段：Docker 一键部署

- [x] 增加正式 `Dockerfile`
- [x] 增加正式 `compose.yaml`
- [x] 增加 `.env.example`
- [x] 让数据库与仓库缓存目录默认持久化到宿主机目录
- [x] 将 Docker 启动收敛为单条主命令

## 第四阶段：文档与引导

- [x] 重写 README，优先面向终端用户而不是开发者
- [x] 增加本地启动说明
- [x] 增加 Docker 部署说明
- [x] 增加常见问题、升级与排障说明
